#include <stdio.h>


/*main function that has a integer return*/
/*you must have a main function*/

int main()
{
	/* Another hello world, only this time in C. */
	printf("This is another hello world program only this time in C. \n");
	printf("Main function that has a integer return. \n");
	printf("You must have a main function. \n");
	return 0; /*Sends back something. 0 means once the main function runs completely, it will return a 0, if not it will be greater than 0*/
}
